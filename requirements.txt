# Core model + extraction
torch>=2.2
transformers>=4.44
accelerate>=0.30
safetensors

# Data + HF
datasets>=2.20
huggingface_hub>=0.24
pandas
pyarrow
numpy
tabulate        # required by DataFrame.to_markdown (manifold/sweep_report.py)

# Exploratory analysis
scikit-learn>=1.6  # hdbscan >=0.8.40 needs ensure_all_finite (renamed in 1.6)
scikit-dimension>=0.3
umap-learn>=0.5
hdbscan>=0.8
ripser>=0.6          # Vietoris-Rips persistent homology (per-role topology)
networkx>=3.0        # kNN graphs for discrete curvature
pot>=0.9             # exact discrete optimal transport, for Ollivier-Ricci

# Steering track (steering/) — the LLM judge, Appendix D.1.3 rubric.
# Optional: every other module imports without it, and the judge is off unless
# a key is present. Only steering/judge.py touches it.
anthropic>=0.40

# Plots
matplotlib
plotly
tqdm
